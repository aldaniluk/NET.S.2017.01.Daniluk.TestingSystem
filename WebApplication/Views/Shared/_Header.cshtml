<div class="navbar navbar-inverse navbar-fixed-top">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            @Html.ActionLink("Around the World", "Index", "Home", new { area = "" }, new { @class = "navbar-brand" })
        </div>
        <div class="navbar-collapse collapse">
            <ul class="nav navbar-nav">
                @if (User.Identity.IsAuthenticated)
                {
                    <li>@Html.ActionLink("Profile", "Profile", "Home")</li>
                }
                <li>@Html.ActionLink("Tests", "Index", "Test")</li>
                @*@if (User.IsInRole("admin"))
                {
                    <li>@Html.ActionLink("Tests (admin)", "All", "Test")</li>
                }*@
                <li>@Html.ActionLink("Statistics", "Index", "Statistic")</li>
            </ul>
        </div>
    </div>
</div>
